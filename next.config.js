/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["i.ibb.co", "i.gr-assets.com", "www.goodreads.com"],
  },
};

module.exports = nextConfig;
